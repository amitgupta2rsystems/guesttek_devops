diff --git a/build-docker.sh b/build-docker.sh
index f7a83de..d4986de 100755
--- a/build-docker.sh
+++ b/build-docker.sh
@@ -2,31 +2,41 @@
 # Build script for Alert Manager Service Docker image
 # Usage: ./build-docker.sh
 
-set -e
+set -euo pipefail
+
+PLATFORM="${PLATFORM:-linux/arm64}"
 
 TODAY=$(date +%Y%m%d)
 TAG="alert-manager-${TODAY}"
-LOCAL_REGISTRY_IMAGE="goby.intercept.ing:5000/wasp:${TAG}"
+IMAGE_REF="camsuite:${TAG}"
 
 SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
 
+EXTRA_ARGS=()
+if [[ "${PLATFORM}" == *","* ]]; then
+  echo ""
+  echo "Multi-arch build: using --push (configure buildx output destination)."
+  EXTRA_ARGS+=(--push)
+else
+  EXTRA_ARGS+=(--load)
+fi
+
 echo ""
-echo "Building Docker image: ${LOCAL_REGISTRY_IMAGE}"
-echo "Platform: linux/arm64"
+echo "Building Docker image: ${IMAGE_REF}"
+echo "Platform(s): ${PLATFORM}"
 echo ""
 
 docker buildx build \
-  --platform linux/arm64 \
-  -t "${LOCAL_REGISTRY_IMAGE}" \
+  --platform "${PLATFORM}" \
+  "${EXTRA_ARGS[@]}" \
+  -t "${IMAGE_REF}" \
   "${SCRIPT_DIR}"
 
 echo ""
 echo "Build complete!"
 echo ""
-echo "Image: ${LOCAL_REGISTRY_IMAGE}"
-echo ""
-echo "To push to local registry:"
-echo "  docker push ${LOCAL_REGISTRY_IMAGE}"
+echo "Image: ${IMAGE_REF}"
 echo ""
+
 echo "To run with docker compose:"
-echo "  docker compose up -d"
+echo "  docker compose up -d"
\ No newline at end of file
